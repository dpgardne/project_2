<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Photos home</title>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
          <a class="navbar-brand" href="/">Home</a>
          <a class="navbar-brand" href="/photos">Explore</a>
          <a class="navbar-brand" href="/users">Users</a>
          <a class="navbar-brand" href="/photos/new">Post new issue</a>
          <a class="navbar-brand" href="#">Contact</a>
          <button class=" this_btn btn btn-default" type="button" aria-haspopup="true" aria-expanded="false">
          <a href="/sessions/new" role="button">Sign in </a>
          </button>
      </div>
    </nav>
    <div class="container_photos">
    <h2 class="jumbo_header">Recently submitted photos</h2>
				<% for(let i = 0; i < photos.length; i++){ %>
          <div class="row">
              <div class="col-sm-6 col-md-4 photo_col">
              <div class="thumbnail">
                <img src="<%=photos[i].image%>" />
            <div class="caption">
						 <h2><a href="/photos/<%=photos[i].id%>"><%=photos[i].name%></a></h2>

            <p><span class="description_span">Address: </span><%=photos[i].address%></p>
            <p><span class="description_span">Description: </span><%=photos[i].description%></p>
            </div>
          </div>
        </div>
      </div>
				<% } %>
      </div>
  </body>
</html>
